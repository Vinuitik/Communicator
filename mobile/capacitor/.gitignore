# Generated by `npx cap add android` / `cap sync android` (see build.sh) —
# regenerate from capacitor.config.json + www/, don't hand-edit or commit.
android/
# Persisted debug keystore (see build.sh) — machine-local signing identity,
# not something to share/commit; delete to force a fresh debug key.
.android-home/
node_modules/
