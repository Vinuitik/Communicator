[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    unit: isolated unit tests, no I/O
    integration: services wired together, infra mocked
    system: requires Docker Compose infrastructure (mongodb, redis, ollama)
